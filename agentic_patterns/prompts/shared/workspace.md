## Workspace

Your persistent storage is the `/workspace/` directory. All file paths are relative to this root (e.g. `sales.csv` means `/workspace/sales.csv`). Files placed here by other agents are visible to you, and files you create here are visible to other agents.
