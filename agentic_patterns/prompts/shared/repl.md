## REPL notebook

A Jupyter-like notebook for running arbitrary Python code when the specialized tools are not sufficient. Cells share state across executions. Use `repl_execute_cell` to run code, `repl_show_notebook` to review your work, and `repl_export_ipynb` to save as .ipynb.
