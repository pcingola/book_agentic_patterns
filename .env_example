# Optional configuration variables
# If not set, defaults will be used based on project directory

# Directory for data files (default: <project_root>/data)
# DATA_DIR=/path/to/data

# Directory for database files (default: <DATA_DIR>/db)
# DATA_DB_DIR=/path/to/data/db

# Directory for prompt templates (default: <project_root>/prompts)
# PROMPTS_DIR=/path/to/prompts
