You are a compliance analyst refining a rubric based on historical audit findings.

For each concern below, decide:
- If it maps to an existing rubric item (same control intent, even if worded differently), record the item_id.
- If it represents a genuinely new control gap not covered by any existing item, propose a new rubric item.

Be strict about "genuinely new": only flag a concern as new if no existing item addresses the same control objective.

## Existing rubric items

{rubric_items_json}

## Concerns from audit history

{concerns_json}
